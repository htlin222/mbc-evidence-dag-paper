% v3 Round 3 Methods Review

**Reviewer role:** adversarial methods reviewer (cycle 9 across the program; cycle 3 of v3)
**Targets:** Paper A (`manuscript/paper_A_clinical_v3.tex`, JCO Precision Oncology) and Paper B (`manuscript/paper_B_methods_v3.tex`, Research Synthesis Methods).
**Materials reviewed:** R1 + R2 methods reviews (`reviews/v3_round1/methods.md`, `reviews/v3_round2/methods.md`); R1-integration commit `4ff3cdd`; R2-integration commit `dadf797` ("v3 R2 integration: sweep 14 stale numbers + regen figures + sync cover letters + sync medRxiv kit"); current `paper_A_clinical_v3.tex` (172 lines), `paper_B_methods_v3.tex` (174 lines); released `data/results/v3_pooled_efdpr.json`; `data/processed/v3_nsclc_full.json`; code `analysis/v3_05_merge_and_kappa_nsclc.py`, `v3_06_encode_nsclc_guideline.py`, `v3_07_build_combined_dag.py`, `v3_08_compute_pooled_efdpr.py`, `v3_09_figures.py`; figure PDFs at `figures/v3_fig{1,2,3}*.pdf` with pdftotext content extraction; release artefacts at `release/`; `git log dadf797..HEAD` (zero commits — R2 integration is the head).

The R2-integration commit `dadf797` did genuine, partial work: it swept 9 numeric replacements in Paper A (subsection 2.2 corpus 180 → 178; subsection 2.4 tree 25 → 24 nodes with 12 → 17 EGFR + 8 → 7 ALK breakdown; subsection 2.5 DAG 210 → 212 edges, 145 → 147 NSCLC in-scope, 47 → 48 source nodes; Figure 1 caption $P=0.0004$ → $P=0.023$; Figure 2 caption "50-node" → "49-node"; Figure 3 caption $P=0.0004$ → $P=0.023$; Discussion headline $P=0.0004$ → $P=0.023$ and "Roughly half" → "Roughly four in ten"; What-this-means paragraph "24 evidence-free" → "19 evidence-free"; corpus snapshot 261 → 259), it synced the Paper B medRxiv kit (cover letter and metadata both now read $P = 0.023$, 49-node, 178 trials), and the body abstract / Key Objective box / Methods §2.5 sentence "combined DAG has 212 edges" are all now self-consistent. **However, the R2 commit's figure regeneration was bogus — the three v3 figure PDFs are byte-identical-sized to the pre-R2 versions and `pdftotext` confirms Figure 1 still carries the title "Pooled mBC + NSCLC EFDPR — pre-registered primary test rejects H₀ (P = 0.0004)" with n=50 / n=25 NSCLC / n=8 ALK row labels, Figure 2 still carries the title "Per-node trial-edge support across pooled 50-node guideline tree", and Figure 3 still plots the v3 point at EFDPR 0.48 with "P < 0.001". The figure-generating script `analysis/v3_09_figures.py:83,121,141-143` still hard-codes the dead values (line 83: `ax.set_title("...P = 0.0004")`; line 121: `"...50-node guideline tree"`; lines 141–143: `efdprs = [0.31, 0.40, 0.48]`, `pvals = [0.37, 0.071, 0.0004]`, and the v3 stage label `"n=50"`). The R2 captions advertise the new numbers; the rendered figures still show the old ones. A reader who looks at the captions vs the figure title will catch the contradiction in 5 seconds.** Independently, R2 asks 1 (8 stale numeric claims), 2 (PAPILLON reproducibility), 3 (NCT-verification gate), 5 (EGFR-mutant "12 evidence-free of 17"), 6 (drug-class PABAK in Paper A), 7 (validation-subset N = 13), 8 (release tag annotation and v3.0.0 release notes), 9 (missing `supplement_v3.tex`), 10 (single-annotator NSCLC tree), 11 (token + LLM-config disclosure), and 12 (filter-step "176 candidates" verification) remain unresolved or only partially addressed. Of R2's 12 asks, **3 are fully fixed (asks 1-partial-via-sweep, 4-headline-P-already-correct-in-abstract, the body of Paper B is self-consistent), 1 is bogus-fixed (figures regen claim, asks 1+4 jointly), and 8 are still open.**

The R2 ask 7 N=15 → N=13 fix and R2 ask 6 drug-class PABAK disclosure in Paper A are particularly visible because Paper B `:127` discloses all three κ failures plus the v3.0.1 deferral, while Paper A `:84` STILL claims "15-trial validation subset" and "kappa gate failed pre-adjudication on `post_alk_tki` and `egfr_t790m`; other fields passed" — i.e., a JCO PO reviewer reading Paper A in isolation will not know about the third failure, will not know the validation N was actually 13, and will not know an NSCLC adjudication pass is deferred to v3.0.1.

The asks below are ordered by severity. Six of eight are blocking for the resubmission; the remainder are revise-with-acknowledgment.

---

## 1. Figures still render the dead numbers (R2 ask 1 + ask 4 jointly, partially regressed; blocking)

1. **ACTION: Edit `analysis/v3_09_figures.py` at lines 83, 121, 141–143 to source the v3 trajectory's third stage and the Figure 1 title from `EFDPR["primary"]["strict"]` rather than hard-coded literals, then re-render and confirm all three v3 figure PDFs change content (not just metadata).** Concretely:
   - Line 83 — `ax.set_title("Pooled mBC + NSCLC EFDPR — pre-registered primary test rejects H₀ (P = 0.0004)", pad=20)` → either parameterise to `f"...rejects H₀ (P = {EFDPR['primary']['strict']['exact_p_one_sided_vs_p25']:.4f})"` or hard-replace with `P = 0.0231`.
   - Lines 44–48 — the row labels `"Primary: pooled mBC + NSCLC (n=50)"`, `"Sensitivity: NSCLC-only (n=25)"`, `"Sensitivity: NSCLC ALK-only (n=8)"` → `n=49`, `n=24`, `n=7` to match the de-duplicated tree.
   - Line 121 — `ax.set_title("Per-node trial-edge support across pooled 50-node guideline tree", pad=8)` → `"... 49-node ..."`.
   - Lines 141–143 — `stages = [..., "v3.0.0\nmBC+NSCLC pooled\n(n=50)"]`, `efdprs = [0.31, 0.40, 0.48]`, `pvals = [0.37, 0.071, 0.0004]` → `n=49`, `0.39`, `0.023` for the v3 stage. The v1/v2 stages should also be cross-checked against `data/results/v1_*.json` and `v2_*.json` rather than left as literals.
   - After editing, run `python3 analysis/v3_09_figures.py` and verify `pdftotext figures/v3_fig1_forest.pdf -` shows "P = 0.023" and "n=49", `pdftotext figures/v3_fig2_per_node.pdf -` shows "49-node", and `pdftotext figures/v3_fig3_trajectory.pdf -` shows the v3 point at "0.39" with "P = 0.023".

   The R2 commit `dadf797` claims "regen figures" but `git show --stat dadf797` shows the three figure PDFs change from `30662 → 30662`, `22122 → 22122`, `21311 → 21311` bytes — byte-identical sizes, content unchanged. The Paper A figure captions were updated to the new numbers; the figures themselves were not. This is the canonical "stale figure, fresh caption" defect class.

## 2. Paper A still has six stale numeric claims after the R2 sweep (R2 ask 1 partially addressed; blocking)

2. **ACTION: Replace the following six remaining stale numeric claims in `paper_A_clinical_v3.tex` because each contradicts the abstract (line 50) or the released JSON:**
   - **Line 81 (Methods §2.2 arithmetic):** "176 systematic candidates. Five reviewer-flagged supplementary pivotal trials (PROFILE 1014, AURA3 ..., HERTHENA-Lung01 ..., TROPION-Lung01 ..., plus one trial removed for misidentification) brought the final NSCLC corpus to 178 trials, of which 145 entered the in-scope trial-DAG." The 176 + 5 − 1 = 180 arithmetic still doesn't give 178; the merge code only hand-curates 3 supplementaries (not 5) and drops 4 misidentified NCTs (not 1) per `v3_05_merge_and_kappa_nsclc.py:18,121`. Also, "145 entered the in-scope trial-DAG" contradicts line 90 which says "147 NSCLC in-scope". Rewrite to the actual pipeline arithmetic, e.g., "176 systematic candidates plus 3 hand-curated round-1 corrections (AURA3 NCT02151981, HERTHENA-Lung01 NCT04619004, TROPION-Lung01 NCT04656652) plus 1 manually-injected supplementary (PAPILLON NCT04538664 added post-hoc; see Reproducibility note below) minus 4 misidentified NCTs dropped at merge yielded the final NSCLC corpus of 178 trials, 147 of which entered the in-scope trial-DAG after the drug-class filter."
   - **Line 84 (Methods §2.3):** "Annotator B (Codex / GPT-5) on a 15-trial validation subset" → 13 (`v3_nsclc_kappa.json:n_validation = 13`; merge code `v3_05:138` keeps only `val_subset ∩ claude_by_nct ∩ codex_by_nct`); and "kappa gate failed pre-adjudication on `post_alk_tki` ... and `egfr_t790m` ...; other fields passed" → add the third failure: `drug_class` (PABAK = 0.54, Cohen κ = 0.72) per Paper B `:127`. Either say "kappa gate failed pre-adjudication on `post_alk_tki`, `egfr_t790m`, and `drug_class` (PABAK = 0.54); a formal NSCLC adjudication pass is deferred to v3.0.1" (parallel to Paper B) or refine the gate definition. Both R1 ask 5/6 and R2 ask 6/7 flagged this; still unaddressed in Paper A.
   - **Line 90 (Methods §2.5):** "the pooled EFDPR is computed on the 50-node pooled denominator with the pre-registered one-sided exact binomial test as primary" → 49-node (the same sentence's first half correctly says 48 canonical source nodes / 212 edges / 147+65 in-scope; the second half still reads "50-node"). This is a one-line edit that the R2 sweep missed.
   - **Line 110 (Figure 3 caption body):** "The pooled 50-node test rejects at $P = 0.023$" → 49-node. The R2 sweep updated the P-value but missed the node count.
   - **Line 133 (Discussion headline):** "At adequately-powered multi-tumor scale (50 pooled guideline nodes across mBC HR+/HER2- and NSCLC EGFR + ALK), the pre-registered exact-binomial test rejected $H_0: \mathrm{EFDPR} \le 0.25$ at $P = 0.023$." → 49 pooled guideline nodes. The P-value was updated; the denominator literal "50" was not.
   - **Line 136 (Discussion EGFR cluster):** "Of 17 EGFR-mutant decision nodes, 12 are evidence-free at strict tolerance." → 7 evidence-free (per `v3_pooled_efdpr.json:nsclc_egfr_only.strict.evidence_free_count = 7`; nodes are N9, N10, N12, N21, N22, N23, N24). R2 ask 5 specifically flagged this as a blocking defect; still unaddressed. The numeric claim is off by 5/17 = 29 percentage points and a reader who opens the JSON catches it in 30 seconds.
   - **Line 139 (Discussion ALK):** "ALK-rearranged decisions ... failed to reject ($P = 0.63$)" → $P = 0.56$ per `v3_pooled_efdpr.json:nsclc_alk_only.strict.exact_p = 0.5551`. The abstract (line 50) correctly reads "NSCLC ALK-only 0.29 ($P = 0.56$)"; the Discussion contradicts the abstract by ~0.07 in P-value.
   - **Line 145 (Caveats):** "First, ESCAT-aligned and liberal tolerance gave $P = 0.10$ on the pooled denominator." → $P = 0.084$ per `v3_pooled_efdpr.json:primary.escat.exact_p = 0.0836`. The abstract (line 50) correctly says "($P = 0.084$, marginal non-rejection)"; the Discussion's first caveat sentence still reads $P = 0.10$.

   These are not "polish" — seven Paper A numeric claims directly contradict the abstract or the released JSON, and three of them (lines 90, 110, 133) say "50-node" within sentences whose other half says "49-node" or "212 edges" (which only makes sense on a 49-node pooled denominator). The R2 sweep was a partial pass; a full pass needs to grep for every literal in {50, 0.10, 0.63, 12, 145, 0.0004, 24} and re-check.

## 3. Paper B `:124,130` still echo "180 trials" and "0.34 (24/50 → 17/50; P = 0.10)" (R2 ask 1B unaddressed; blocking)

3. **ACTION: Replace two stale numeric claims in `paper_B_methods_v3.tex`:**
   - **Line 124:** "702 candidates were filtered to 180 trials (145 in-scope)" → 178 trials (147 in-scope). The Paper B abstract (line 48) correctly says "pooling 259 systematically-searched pivotal trials"; the Demonstration §6.1 says 180/145 — i.e., the body contradicts its own abstract by 2 trials.
   - **Line 130 (Tolerance-sensitivity grid):** "ESCAT-aligned and liberal tolerance gave 0.34 (24/50 $\to$ 17/50; $P = 0.10$), failing to reject." → 0.35 (19/49 $\to$ 17/49; $P = 0.084$). The Paper B abstract correctly says "ESCAT and liberal tolerance gave 0.35 ($P = 0.084$)"; §6.3 contradicts the abstract on both the point estimate (0.34 vs 0.35), the count (24/50 → 17/50 vs 19/49 → 17/49), and the P-value (0.10 vs 0.084). The §6.2 paragraph at line 127 says the right numbers; §6.3 immediately following contradicts them.

   These are the same defect class as Paper A ask 2: the R2 sweep updated some literals and missed others, leaving the manuscript internally contradictory.

## 4. PAPILLON injection still not reproducible from the merge script (R1 ask 11, R2 ask 2; blocking)

4. **ACTION: Add PAPILLON (NCT04538664) as a fourth entry in `HAND_CURATED_SUPPLEMENTARIES` in `analysis/v3_05_merge_and_kappa_nsclc.py:18-97`, OR document explicitly in Paper A §2.2 and Paper B §6.1 that `v3_nsclc_full.json` contains one record (PAPILLON) injected outside the canonical merge pipeline and provide the script that injects it.** R2 ask 2 (and R1 ask 11) specifically demanded this. Verification: running `python3 analysis/v3_05_merge_and_kappa_nsclc.py` regenerates `data/processed/v3_nsclc_full.json` with **177 trials**, PAPILLON missing. The committed JSON has 178 trials with PAPILLON (`provenance = "nsclc_v3_round1_added"`). The R1-integration commit and the R2-integration commit both left this unaddressed; the released `v3.0.0` artefact remains non-reproducible from the released code. This breaks Paper B's explicit reproducibility commitment (`paper_B_methods_v3.tex:155-156` "The tagged commit reproduces every figure and table"). The minimal fix is to add a fourth dict to `HAND_CURATED_SUPPLEMENTARIES` for PAPILLON; the wider fix is to also add the PAPILLON-regex `\bex20ins\b|\bexon.?20.?insertion|\bEGFR.?ex20` recovery into the systematic pipeline so PAPILLON enters as `provenance = "batch"`, not as a hand-injection.

## 5. NCT-verification gate, supplement_v3.tex, single-annotator NSCLC-tree disclosure, token + LLM-config disclosure all still missing (R1 asks 7-8, 11, 12-13, 16; R2 asks 3, 9, 10, 11; blocking as a set, revise individually)

5. **ACTION: Address the four R1+R2 long-standing disclosure gaps as a single batch.** The R2 commit did not touch any of these; they will block a Research Synthesis Methods desk-editor review even if every other ask is fixed.
   - **(5a) NCT-verification gate (R2 ask 3, R1 ask 11):** Add to `paper_B_methods_v3.tex` §3 (Dual-LLM-annotator extraction pipeline) a new subsection or paragraph explicitly committing to a "Trial-Name-to-NCT verification gate" with the 4-step procedure (fetch briefTitle for asserted NCT; confirm acronym substring; flag for human review; record outcome). At minimum, add to Paper A `:148` Discussion "Three positives" paragraph the explicit forward commitment that v3.0.1 / v4 will not require a 7th NCT-correction round.
   - **(5b) `manuscript/supplement_v3.tex` (R2 ask 9, R1 ask 16):** Create the file. Paper A `:90` cites "Supplementary Table S1" as if it exists; only `supplement.tex` (v1) and `supplement_v2.tex` (v2) are on disk. Minimum content: S1 PRISMA-2020 checklist with graph-encoding addendum; S2 per-NCT NSCLC corpus with extraction flags; S3 NSCLC 24-node decision-tree encoding with sources; S4 NSCLC κ / PABAK by field (3 of 8 fields below 0.70); S5 per-node `Supp(g)` set from `v3_pooled_efdpr.json:per_node_support`.
   - **(5c) Single-annotator NSCLC-tree disclosure (R2 ask 10, R1 asks 7-8):** Add to `paper_A_clinical_v3.tex` §2.4 (Methods) one sentence acknowledging that the 24-node NSCLC decision tree (`analysis/v3_06_encode_nsclc_guideline.py:23-279`) was hand-encoded by a single annotator (Claude) without independent re-encoding by Codex or external clinical adjudicator, and name this as a v3.0.1-deferred adjudication item.
   - **(5d) Token + LLM-config disclosure (R2 ask 11, R1 asks 12-13):** Add to `paper_B_methods_v3.tex:149-150` (Reproducibility and AI transparency) either (a) Supplementary Table T1 with model version, batch size, mean input/output tokens per trial, total tokens, USD cost, wall-clock time, temperature, top-p, max_tokens, retry policy; or (b) honest paragraph stating "token counts, per-extraction USD cost, and LLM call configuration (temperature, top-p, max_tokens, retry policy) were not retained from the v3 batch runs; future versions will instrument this." The latter is weak but acceptable; the former is what RSM 2025 AI/ML guidance expects.

## 6. Release tag annotation and `release/RELEASE_NOTES_v3.0.0.md` + `release/submission_plan_paperA_jcopo/*` still encode "P = 0.0004" / "n=50" / "EFDPR 0.48" (R2 ask 8 unaddressed; blocking for release notes)

6. **ACTION: Cut `v3.0.1` with annotation text reflecting the post-R1/R2 numbers, and update `release/RELEASE_NOTES_v3.0.0.md` lines 12, 26, 47, 61 plus `release/submission_plan_paperA_jcopo/AUDIT_REPORT.md:15,42` and `release/submission_plan_paperA_jcopo/POLISH_PLAN_14_DAYS.md:73,82` to the actual numbers.** Current `v3.0.0` tag annotation still reads "v3.0.0 — multi-tumor pooled REJECTS H0 at P=0.0004 (citation-polished) 50-node pooled mBC + NSCLC EGFR/ALK; strict-tolerance EFDPR 0.48, exact P = 0.0004 rejects H0:EFDPR<=0.25." Commit `11abc41` subject line still reads "v3: NSCLC corpus (180 trials) + dual-annotator extraction". The R2 commit synced the Paper B medRxiv kit (`release/submission_kit_paperB_medrxiv/cover_letter_medrxiv.md` and `medrxiv_metadata.md` both now read $P = 0.023$, 49-node, 178 trials — verified) but did not touch the GitHub release notes or the JCO PO submission plan. Recommend cutting `v3.0.1` with annotation "v3.0.1 — multi-tumor pooled REJECTS H0 at P = 0.023 (strict); ESCAT/liberal P = 0.084 (marginal non-rejection); 178 NSCLC + 81 mBC trials, 49-node pooled tree after R1 dedup."

## 7. Drug-class table fix uses the wrong direction: SAVANNAH and the ex20ins TKIs still classified `"investigational (other)"` in extractions (R1 ask 9 partially addressed; revise)

7. **ACTION: Decide what the R1 ask 9 fix is actually trying to do. The R1-integration commit added three entries to `DRUG_CLASS_EQUIVALENCE` in `analysis/v3_08_compute_pooled_efdpr.py:135-139` (`MET-TKI + osimertinib`, `EGFR TKI mutant-selective (ex20ins)`, `investigational (other)`) AND made `investigational (other)` a member of both new sets. But the corpus extractions (`v3_nsclc_full.json`) still label SAVANNAH (NCT03778229), ORCHARD (NCT03944772), WU-KONG1 (NCT03974022), REZILIENT1 (NCT04036682), EXCLAIM-2 (NCT04129502), AZD3759-BM (NCT03653546), and PAPILLON (NCT04538664) as `drug_class = "investigational (other)"` rather than `MET-TKI + osimertinib` / `EGFR TKI mutant-selective (ex20ins)` / `Amivantamab monotherapy`. The "fix" was made on the matcher side: guideline nodes N12 and N21 now use `recommended_classes = ["investigational (other)"]` (lines 134-141 of `v3_nsclc_decision_tree.json`) — i.e., the guideline node is relabelled to match the trial's catch-all class, not the other way round. This is structurally backward: a guideline node's `recommended_classes` should reflect the actual ESMO/ASCO/NCCN recommendation (savolitinib + osimertinib; mobocertinib / sunvozertinib / zipalertinib; amivantamab monotherapy), not the corpus's catch-all label.** Two correct ways to handle this: (a) re-extract the ~7 NSCLC trials with corrected canonical drug-class labels (SAVANNAH → `MET-TKI + osimertinib`; the ex20ins TKIs → `EGFR TKI mutant-selective (ex20ins)`; PAPILLON → `Amivantamab + chemotherapy` per its current N11 cited-trial) AND restore N12/N21 to their proper guideline-recommended classes; (b) document in Methods that the canonical drug-class extraction did not distinguish these classes from "investigational (other)" and that the matcher's equivalence-table augmentation is the agreed-upon compromise. Either way, N9, N12, N21 remain evidence-free under strict, and the strict EFDPR remains 0.39 — but the *reason* is now "the canonical extraction collapsed several distinct drug classes into one bucket", not "the framework correctly identified evidence gaps". Disclose this honestly.

## 8. Three of four pre-adjudication κ failures, validation N = 13 ≠ 15, single-annotator NSCLC-tree, and PAPILLON-without-NCT-in-N11 should be folded into a unified "Methodological caveats" paragraph in Paper A (revise; consolidation)

8. **ACTION: Add a fourth paragraph to Paper A Discussion §"Three caveats" (or rename to "Four caveats") at `paper_A_clinical_v3.tex:144-145` collecting the four pre-existing methodological caveats that are scattered across Paper B but invisible in Paper A:** (i) NSCLC adjudication pass deferred to v3.0.1 (third κ failure on `drug_class` PABAK = 0.54); (ii) Codex/Annotator-B validation overlap was N = 13, not the pre-registered N = 15 (2 dropouts unexplained); (iii) the 24-node NSCLC decision tree was hand-encoded by a single annotator (Claude); (iv) PAPILLON is injected post-merge and is not regenerable from `v3_05_merge_and_kappa_nsclc.py` alone (R1 ask 11 / R2 ask 2). A JCO PO reviewer reading Paper A in isolation should see all four; currently they will see none.

---

## Verdict

**MAJOR (continuing from R1, R2)**

The R2-integration commit `dadf797` swept ~9 numeric replacements in Paper A and synced the Paper B medRxiv kit, demonstrably reducing the stale-number contradiction count from R2's ~10 to R3's ~7 in Paper A and from R2's 2 to R3's 2 in Paper B. The Paper A abstract (line 50) and Key Objective box (line 61) are now internally consistent with the released `v3_pooled_efdpr.json` to 4 decimal places. The Paper B body (line 127) discloses the third κ failure (`drug_class` PABAK = 0.54) and the v3.0.1 deferral — these are honest and well-written. The framework, the strict-tolerance rejection at $P = 0.023$, and the prereg-committed-before-data structure are real. However: (i) the figure regeneration was bogus — three v3 figure PDFs are byte-identical-sized to pre-R2 and `pdftotext` confirms Figure 1's title still reads "P = 0.0004" with n=50/n=25/n=8 row labels, Figure 2 still says "50-node", Figure 3 still plots v3 at EFDPR 0.48 / P < 0.001; the figure script `v3_09_figures.py:83,121,141-143` still hard-codes the dead literals; (ii) Paper A retains seven stale numeric claims at lines 81, 84, 90, 110, 133, 136, 139, 145 that contradict its own abstract; (iii) Paper B retains two stale numeric claims at lines 124 and 130; (iv) PAPILLON injection still not reproducible from the merge script (`python3 v3_05_merge_and_kappa_nsclc.py` produces 177 not 178 trials); (v) `manuscript/supplement_v3.tex` still missing; (vi) NCT-verification gate, single-annotator NSCLC-tree disclosure, and token + LLM-config disclosure all still missing from Paper B; (vii) the GitHub `v3.0.0` release tag annotation and `release/RELEASE_NOTES_v3.0.0.md` and `release/submission_plan_paperA_jcopo/*` still encode "P = 0.0004" / "n=50" / "EFDPR 0.48"; (viii) the drug-class table fix uses the wrong direction (guideline nodes relabelled to corpus catch-all rather than canonical extraction relabelled to ESMO recommendation).

The minimum blocking fix-set is asks 1 (figure script + re-render), 2 (Paper A seven stale literals + Paper A §2.3 third κ failure + N = 13), 3 (Paper B two stale literals), 4 (PAPILLON in `HAND_CURATED_SUPPLEMENTARIES`), and 6 (release notes + tag). Asks 5 (NCT gate + supplement + single-annotator + token/LLM disclosure) and 7 (drug-class direction) and 8 (Paper A caveats consolidation) can be addressed as revise-with-acknowledgment.

**A v3 round-4 methods pass is warranted.** The R2 sweep was a partial pass with no figure-script edit and no script-side reproducibility check; R3 should not be the last opportunity to catch "figure title still says P = 0.0004" because that is exactly the class of defect a JCO PO desk-editor catches in the first 60 seconds.
