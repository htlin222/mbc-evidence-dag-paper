# v3 Round 2 Methods Review

**Reviewer role:** adversarial methods reviewer (cycle 8 across the program; cycle 2 of v3)
**Targets:** Paper A (`manuscript/paper_A_clinical_v3.tex`, JCO Precision Oncology) and Paper B (`manuscript/paper_B_methods_v3.tex`, Research Synthesis Methods).
**Materials reviewed:** v3 Round-1 review `reviews/v3_round1/methods.md`; R1-integration commit `4ff3cdd` ("v3 R1 integration: fix 6 critical bugs + bootstrap CI + NSCLC citations + paper updates"); current `paper_A_clinical_v3.tex` (172 lines), `paper_B_methods_v3.tex` (174 lines); released artifacts `data/results/v3_pooled_efdpr.json`, `data/results/v3_nsclc_kappa.json`, `data/processed/v3_nsclc_full.json` (178 records), `v3_nsclc_decision_tree.json` (24 nodes), `v3_combined_dag_edges.json` (212 edges), `v3_combined_dag_nodes.json` (48 nodes); code `analysis/v3_02_filter_nsclc.py`, `v3_05_merge_and_kappa_nsclc.py`, `v3_06_encode_nsclc_guideline.py`, `v3_07_build_combined_dag.py`, `v3_08_compute_pooled_efdpr.py`; release tag `v3.0.0`; commit `11abc41` message; rerun verification of the merge pipeline.

The R1-integration commit `4ff3cdd` did substantive work — biomarker superset matching, FLAURA N1 fix, PAPILLON regex, TROPION-Lung01 NCT routing, N13/N14 deduplication, the brain-mets non-strip, drug-class table extension, bootstrap CI computation, the NSCLC adjudication deferral, NSCLC trial citations — and the released `v3_pooled_efdpr.json` is now self-consistent (strict EFDPR = 0.3878 ≈ 0.39, P = 0.0231 ≈ 0.023, 19/49 evidence-free, n_validation = 13, bootstrap CI = [0.245, 0.531]). R1 asks 4, 5, 9 (drug-class), 11 (PAPILLON), 14 (PABAK conflation rewrite) are demonstrably addressed in code or in Paper B. **However, the same integration commit left Paper A peppered with stale v2/v3.0.0-era numbers and prose inconsistent with the new JSON, introduced a new reproducibility breach (the released `v3_nsclc_full.json` is no longer reproducible from `v3_05_merge_and_kappa_nsclc.py`), and silently changed the headline result class (from "strict-only rejection at P = 0.0004" to "strict rejection at P = 0.023 with ESCAT marginal at P = 0.084") without removing the pre-integration framing.** Independently, R1 asks 1 (corpus arithmetic), 2 (strict-vs-ESCAT contrast in Results), 6 (15-trial → 13-trial validation N), 7-8 (single-annotator NSCLC tree), 11 (NCT verification gate), 12-13 (token-count + LLM-config disclosure), and 16 (missing `supplement_v3.tex`) are still unresolved or partially-addressed. The release tag annotation and commit `11abc41` message still encode the dead "180 trials" / "P = 0.0004" framing.

The asks below are ordered by severity. Eight of twelve are blocking for the resubmission; the remainder are revise-with-acknowledgment.

---

## 1. Internal arithmetic contradiction across Paper A (blocking)

1. **ACTION: Resolve at least eight stale numerical claims in `paper_A_clinical_v3.tex` that contradict the abstract (line 49–50) and the released JSON. The abstract correctly states 178 NSCLC pivotal trials, 49-node tree (25 mBC + 24 NSCLC), 19/49 evidence-free, strict P = 0.023; Methods §2.2 (line 81), Methods §2.4 (line 87), Methods §2.5 (line 90), Figure 1 caption (line 100), Figure 3 caption (line 110), Figure 2 caption (line 123), Discussion Headline (line 133), and Discussion paragraph at line 142 each repeat the dead v3.0.0 numbers.** Specifically:
   - Line 81: "filtered ... to 176 systematic candidates. Five reviewer-flagged supplementary pivotal trials (PROFILE 1014, AURA3, HERTHENA-Lung01, TROPION-Lung01, plus one trial removed for misidentification) brought the final NSCLC corpus to 180 trials, of which 145 entered the in-scope trial-DAG." → corpus is **178**, in-scope NSCLC edges are **147**, the merge code in `v3_05` lists three corrected supplementaries plus PAPILLON injected separately (see ask 2 below); the "176 systematic + 5 − 1 = 180" arithmetic doesn't add up to any number, let alone 178.
   - Line 87: "25 nodes (13 ESMO+ASCO+NCCN-shared, 6 NCCN-unique, 2 ASCO+NCCN, 2 ESMO+NCCN, 1 ESMO-only, 1 ASCO-only). 12 nodes cover EGFR-mutant decisions ... 8 cover ALK-rearranged ... 5 cover combined or special-population scenarios." → tree is **24** nodes (N14 was merged into N13 per the R1 fix at `v3_06:151-162`); EGFR-mutant count is **17**, ALK is **7**, combined/special is **0**. The 13+6+2+2+1+1=25 source-breakdown is also stale.
   - Line 90: "The combined DAG has 210 edges (145 NSCLC in-scope + 65 mBC in-scope) and 47 canonical source nodes. The pooled EFDPR is computed on the 50-node pooled denominator ..." → actual combined DAG has **212 edges** (147 NSCLC + 65 mBC) and **48 canonical source nodes**; pooled denominator is **49**, not 50.
   - Line 100 (Figure 1 caption): "Diamond: primary 50-node analysis ($P = 0.0004$)" → **49-node**, P = 0.023.
   - Line 110 (Figure 3 caption): "The pooled 50-node test rejects at $P = 0.0004$ ($-\log_{10} P = 3.4$)" → 49-node, P = 0.023 ($-\log_{10} P ≈ 1.6$).
   - Line 123 (Figure 2 caption): "Per-node trial-edge support across the pooled 50-node decision tree" → 49-node.
   - Line 133 (Discussion Headline): "At adequately-powered multi-tumor scale (50 pooled guideline nodes across mBC HR+/HER2- and NSCLC EGFR + ALK), the pre-registered exact-binomial test rejected $H_0: \mathrm{EFDPR} \le 0.25$ at $P = 0.0004$" → 49 nodes, P = 0.023. Same paragraph asserts "Roughly half ... lack a single trial edge" but 19/49 = 39%, not "half" — the prose should say "two in five" or "39%".
   - Line 142: "The 24 evidence-free decision nodes are not all equally problematic" → there are **19** evidence-free decision nodes under strict tolerance (line 118 correctly says 19; lines 133 and 142 contradict line 118 within the same Discussion).
   - Line 151: "The 261-trial pooled corpus is a snapshot at freeze 2026-05-16" → **259 trials** (178 NSCLC + 81 mBC) per the abstract.
   - Paper B has analogous stale numbers: line 124 ("180 trials (145 in-scope)" — should be 178 / 147) and line 130 ("ESCAT-aligned and liberal tolerance gave 0.34 (24/50 → 17/50; $P = 0.10$)" — actual escat = 0.35, 17/49, P = 0.084 per `v3_pooled_efdpr.json:primary.escat`; this same line then contradicts Paper B's own line 127 which gives the correct numbers in the preceding paragraph).

   These are not "polish" — eight independent numerical claims in the body of the headline manuscript directly contradict the abstract and the released JSON. An RSM or JCO PO desk-editor will catch the largest one (e.g., "50-node ... P = 0.0004" in Figure 1 caption vs "49-node ... P = 0.023" in abstract) within minutes.

## 2. Reproducibility breach: `v3_nsclc_full.json` cannot be regenerated from code (blocking)

2. **ACTION: Either (a) add PAPILLON (NCT04538664) as a fourth entry in `HAND_CURATED_SUPPLEMENTARIES` in `analysis/v3_05_merge_and_kappa_nsclc.py:18-97`, or (b) document explicitly in Paper B §3 and Methods §2.2 of Paper A that `v3_nsclc_full.json` contains one record (PAPILLON) injected outside the canonical merge pipeline and provide the script that injects it.** Running `python3 analysis/v3_05_merge_and_kappa_nsclc.py` regenerates `data/processed/v3_nsclc_full.json` with **177 trials** (not 178; PAPILLON missing). The committed `v3_nsclc_full.json` has 178 trials, and PAPILLON is present with `provenance = "nsclc_v3_round1_added"`. The provenance string proves the addition was deliberate, but the script that performs the addition is absent from the repository. This breaks the explicit reproducibility commitment in Paper B §"Code Availability Statement" (`paper_B_methods_v3.tex:155-156` "The tagged commit reproduces every figure and table") because re-running the pipeline from scratch produces 177 → 210 edges → 47 nodes → 18/48 evidence-free at strict tolerance — i.e., a numerically different result from what the paper reports (212 edges, 48 nodes, 19/49). This is a worse class of defect than the R1 arithmetic error because the released artifacts (JSON) and the released code (merge script) actively disagree.

## 3. NCT-verification process fix still missing — round 13 misidentification incoming (blocking per R1 ask 11)

3. **ACTION: Add to Paper B §3 (Dual-LLM-annotator extraction pipeline) the explicit "Trial-Name-to-NCT verification gate" described in R1 ask 11, OR add to Paper A `:148` Discussion "Three positives" paragraph an honest extension acknowledging that the v3-round-0 + v3-round-1 cycle alone discovered 4 NCT-misidentifications in `v3_05_merge_and_kappa_nsclc.py:121` and that the pipeline does not yet contain a verification gate to prevent this class of error from recurring.** R1 ask 11 specified a 4-step verification procedure (fetch briefTitle for asserted NCT, confirm acronym substring, flag for human review, record outcome). The R1-integration commit added zero such verification to the codebase: no new `analysis/v3_*_verify_nct.py` exists, and Paper B §3.2 (`paper_B_methods_v3.tex:107-108` "Two-annotator pipeline") still has no language committing to the gate. The Discussion "Three positives" paragraph (Paper A `:148`) actually advertises "the multi-round internal adversarial review process surfaced and corrected 6 NCT-identification errors across v1/v2/v3 with full git-log transparency" — which is honest, but should be paired with the explicit forward commitment that v3.0.1 / v4 will not require a 7th round.

## 4. Headline framing: P = 0.023 vs P = 0.0004 vs ESCAT-marginal must be foregrounded (R1 asks 2-3 partially addressed)

4. **ACTION: Remove "P = 0.0004" everywhere it appears in `paper_A_clinical_v3.tex` (lines 100, 110, 133) and `paper_B_methods_v3.tex` if any, and replace with the actual P = 0.023; reframe Paper A `:50` and `:61` to state "the pre-registered primary test (strict tolerance) rejected $H_0$ at $P = 0.023$; the pre-registered ESCAT-aligned and liberal sensitivity grid gave $P = 0.084$, failing to reject — a non-trivial tolerance dependence that we report as pre-specified."** Paper A line 50 currently says "rejected $H_0$ at $\alpha = 0.05$ ($P = 0.023$). ESCAT-aligned and liberal tolerance gave 0.35 ($P = 0.084$, marginal non-rejection), indicating tolerance sensitivity that we report as pre-registered." — this is much better than the v3-round-0 framing, but the contrast is still soft. Note: the new P = 0.023 vs old P = 0.0004 distinction is HUGE for an RSM reviewer reading the figure caption. The fact that the integration commit changed the headline P by an order of magnitude (because biomarker-superset matching reduced evidence-free count from 24/50 to 19/49) and the figure caption still advertises the old number is a smoking gun for "the figure was rendered from old data and not re-rendered after the bug fix" — independent of whether the figure is correct. Re-render and re-caption.

## 5. Discussion "12 EGFR-mutant evidence-free" claim is internally false (blocking)

5. **ACTION: Rewrite `paper_A_clinical_v3.tex:135-136` because the claim "Of 17 EGFR-mutant decision nodes, 12 are evidence-free at strict tolerance" contradicts the released JSON, which shows 7 evidence-free of 17 EGFR nodes (`v3_pooled_efdpr.json:nsclc_egfr_only.strict.evidence_free_count = 7`; nodes are N9, N10, N12, N21, N22, N23, N24). The correct claim is "Of 17 EGFR-mutant decision nodes, 7 are evidence-free at strict tolerance (post-osimertinib salvage cluster: N9 TROP2-ADC, N10 platinum, N12 ex20ins post-chemo, N21 MET-amp, N22 brain-mets 1L, N23 post-amivantamab, N24 post-osi+post-chemo)."** This is the same class of error as ask 1, but it is a substantive Discussion claim, not a stale figure caption — the reader will check the JSON and find 7, not 12. The R1-integration commit appears to have updated lines 50/61/118 (correct) but missed the Discussion paragraph at 136 that quantifies the EGFR cluster size.

## 6. Paper A drug-class PABAK disclosure asymmetric vs Paper B (R1 ask 5 partially addressed)

6. **ACTION: Update `paper_A_clinical_v3.tex:84` to mirror Paper B `:127` and disclose the third pre-adjudication kappa-gate failure: `drug_class` (Cohen's $\kappa = 0.72$ passes; PABAK = 0.54 fails). Either (a) state explicitly "kappa gate failed pre-adjudication on \texttt{post\_alk\_tki} ($\kappa = 0.29$), \texttt{egfr\_t790m} ($\kappa = 0.61$), and \texttt{drug\_class} (PABAK = 0.54); a formal NSCLC adjudication pass is deferred to v3.0.1", or (b) refine the validation-gate language in both papers to specify that `drug_class` passes by Cohen's-$\kappa$ alone (since the marginals are not skewed enough to invoke the kappa paradox) and explicitly justify the asymmetric criterion for this one field.** Paper B `:127` discloses all three failures plus the v3.0.1 deferral; Paper A `:84` discloses only two. A reader who reads Paper A in isolation (e.g., a JCO PO reviewer) will not know about the third failure or the deferral.

## 7. Validation-subset N = 13 ≠ 15 (R1 ask 6 unaddressed)

7. **ACTION: Change `paper_A_clinical_v3.tex:84` from "Annotator B (Codex / GPT-5) on a 15-trial validation subset" to "Annotator B (Codex / GPT-5) on a pre-registered 15-trial validation subset of which 13 were dually annotated; 2 trials (NCT[s] to be specified) were extracted by Annotator A but not by Annotator B due to [reason]." This was R1 ask 6, unaddressed in the integration commit.** The released `v3_nsclc_kappa.json:n_validation = 13`, and the merge code (`v3_05:138`) explicitly computes `val_ncts = [n for n in val_subset if n in claude_by_nct and n in codex_by_nct]`. Either disclose the 2 dropouts (and which NCTs they were and why), or rerun Annotator B on the full 15-trial subset and reach 15. Saying "15-trial validation subset" when the released artifact says 13 is a fact-check failure that takes a reviewer 90 seconds to catch.

## 8. Release tag annotation and commit `11abc41` message still encode dead numbers (blocking for release notes)

8. **ACTION: Re-tag `v3.0.0` (or cut a `v3.0.1` tag) with annotation text that reflects the post-R1 numbers. Current annotation reads "v3.0.0 — multi-tumor pooled REJECTS H0 at P=0.0004 (citation-polished)"; commit `11abc41` subject line reads "v3: NSCLC corpus (180 trials) + dual-annotator extraction". Both are post-integration-commit stale.** A reviewer who opens the GitHub release page will see "P = 0.0004" and "180 trials" in the release notes within 5 seconds; this is the same defect class as v1.0.0's earlier release notes that v1 round-1 review flagged. Recommend cutting `v3.0.1` with annotation "v3.0.1 — multi-tumor pooled REJECTS H0 at P = 0.023 (strict); ESCAT/liberal P = 0.084 (marginal non-rejection); 178 NSCLC + 81 mBC trials, 49-node pooled tree after R1 dedup."

## 9. `supplement_v3.tex` still missing (R1 ask 16 unaddressed; revise)

9. **ACTION: Create `manuscript/supplement_v3.tex` per R1 ask 16, OR remove the cross-reference in Paper A `:90` ("PRISMA-2020 reporting items are listed in Supplementary Table S1 with a graph-encoding addendum").** No `supplement_v3.*` file exists in `manuscript/`; only `supplement.tex` (v1 pilot) and `supplement_v2.tex` (v2). The cross-reference to "Supplementary Table S1" is a broken pointer. Minimum acceptable: (S1) PRISMA-2020 checklist with graph-encoding addendum; (S2) per-NCT NSCLC corpus with extraction outputs; (S3) NSCLC 24-node decision-tree encoding with sources; (S4) NSCLC κ / PABAK by field with the three gate failures marked; (S5) per-node `Supp(g)` set from `v3_pooled_efdpr.json:per_node_support`.

## 10. Single-annotator NSCLC tree risk still not disclosed (R1 asks 7-8 unaddressed; revise)

10. **ACTION: Add to `paper_A_clinical_v3.tex:86-87` Methods §2.4 the explicit acknowledgment that the 24-node NSCLC decision tree (`analysis/v3_06_encode_nsclc_guideline.py:23-279`) was hand-encoded by a single annotator (Claude) without independent re-encoding by Codex or by an external clinical adjudicator, paralleling the v2 round-3 G6 finding.** The R1-integration commit fixed three encoding issues (N1 token, N13/N14 dedup, brain-mets non-strip — all on the encoder side) but did not introduce a second annotator for the tree, did not run a per-node $\kappa$ on the (state, biomarker, recommended_classes, year) 4-tuple, and did not run the sensitivity analysis R1 ask 7 specified (randomly re-coding 2/24 nodes and reporting EFDPR shift). At minimum, name this as a v3.0.1-deferred adjudication item in the Methods.

## 11. LLM-extraction transparency: token counts + LLM config still missing (R1 asks 12-13 unaddressed; revise)

11. **ACTION: Add to `paper_B_methods_v3.tex:149-150` (Reproducibility and AI transparency) one of: (a) Supplementary Table T1 with model version, batch size, mean input/output tokens per trial, total tokens, USD cost, wall-clock time, temperature, top-p, max_tokens, retry policy; or (b) honest paragraph stating "token counts, per-extraction USD cost, and LLM call configuration (temperature, top-p, max_tokens, retry policy) were not retained from the v3 batch runs; future versions will instrument this."** The R1-integration commit did not add any of this. For a paper whose central methodological pitch is "first published evidence-synthesis tool to pair LLM extraction with formal inter-rater statistics", missing the token + config disclosure is the highest-visibility AI-transparency gap an RSM reviewer can catch.

## 12. Filter-step count "176 candidates" likely stale after the PAPILLON regex fix (revise)

12. **ACTION: Re-verify the filter-pipeline narrative in Paper A `:81`. Re-running `python3 analysis/v3_02_filter_nsclc.py` produces `nsclc_candidate_corpus.json` with `passed_all = 192` (per current `data/processed/nsclc_filter_log.json`), not 176. The "702 candidates filtered to 176" sentence is from the pre-PAPILLON-regex run; the integration commit added `\\bex20ins\\b|\\bexon.?20.?insertion|\\bEGFR.?ex20` to `v3_02_filter_nsclc.py:29` ("v3 round-1 fix: capture PAPILLON-style 'Exon 20ins'") but did not re-run the extraction batches.** Two correct ways to handle this: (a) re-run extraction on the 16 newly-passing trials and report 192-candidate → 197-trial corpus; (b) state in Methods that "the PAPILLON-regex fix was applied to the filter logic in `v3_02_filter_nsclc.py` post-extraction; the resulting 16 newly-passing trials were not re-extracted by Annotator A in time for v3.0.0 freeze; PAPILLON itself was added by hand as a fourth supplementary entry". Either way, the current narrative "702 → 176 systematic + 5 supplementary − 1 = 180" is not arithmetically derivable from any combination of the current code state and the released artifacts.

---

## Verdict

**MAJOR (continuing from R1)**

The R1-integration commit `4ff3cdd` did real technical work — biomarker-superset matching, FLAURA N1 support, PAPILLON entry, TROPION-Lung01 NCT routing, N13/N14 deduplication, brain-mets non-strip, drug-class table extension, bootstrap CI, NSCLC adjudication deferral — and the released `v3_pooled_efdpr.json` is now internally self-consistent at strict EFDPR = 0.39, P = 0.023, 19/49 evidence-free, n_validation = 13. R1 asks 4 (NSCLC adjudication deferral in Paper B), 5 (drug-class PABAK in Paper B), 9 (drug-class table extension), 11 (PAPILLON in corpus), and 14 (PABAK conflation rewrite in Paper B) are addressed. However, the same commit (i) left at least 8 stale numerical claims in `paper_A_clinical_v3.tex` and 2 in `paper_B_methods_v3.tex` that contradict the abstract (lines 81, 87, 90, 100, 110, 123, 133, 136, 142, 151 in A; lines 124, 130 in B) — most prominently the Figure 1 and Figure 3 captions still advertising "P = 0.0004" when the headline is now P = 0.023, the Discussion still claiming "24 evidence-free" when the JSON says 19, and the Discussion still claiming "12 EGFR-mut evidence-free of 17" when the JSON says 7; (ii) introduced a new reproducibility breach (`v3_nsclc_full.json` is no longer regenerable from `v3_05_merge_and_kappa_nsclc.py` — the script reproduces 177 trials without PAPILLON, the released JSON has 178 trials with PAPILLON); (iii) left the `v3.0.0` release tag annotation and commit `11abc41` message encoding the dead "P = 0.0004" / "180 trials" framing; (iv) left R1 asks 1 (corpus arithmetic narrative), 6 (15-trial → 13-trial validation N), 7-8 (single-annotator NSCLC tree disclosure), 11 (NCT-verification process fix), 12-13 (token-count + LLM-config disclosure), and 16 (missing `supplement_v3.tex`) unresolved or only partially addressed.

The minimum blocking fix-set is asks 1, 2, 3, 4, 5, 6, 8 (the seven blocking items above). The recommended fix-set is all twelve. The release tag and commit `11abc41` message both need re-cutting (ask 8). Recommend a major revision and a v3 round-3 methods pass on the resubmission. The framework is real, the rejection is real, the bug fixes were real — but a 2026 reader who opens the PDF and the GitHub release together will catch the contradictions inside 90 seconds.
