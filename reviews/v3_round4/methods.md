% v3 Round 4 Methods Review (final)

**Reviewer role:** adversarial methods reviewer (cycle 10 across the program; cycle 4 of v3 — the final pass).
**Targets:** Paper A (`manuscript/paper_A_clinical_v3.tex`, JCO Precision Oncology) and Paper B (`manuscript/paper_B_methods_v3.tex`, Research Synthesis Methods).
**Materials reviewed:** R1, R2, R3 methods reviews (`reviews/v3_round{1,2,3}/methods.md`); R3-integration commit `123a461`; current `paper_A_clinical_v3.tex` (172 lines), `paper_B_methods_v3.tex` (174 lines); `release/canonical_numbers.md` (R3-new, 79 lines); `release/audit_numbers.sh` (R3-new, 63 lines, **executed and passes**); `analysis/v3_09_figures.py` (R3-edited at lines 44–51, 86–89, 126–127, 144–154); rendered `figures/v3_fig{1,2,3}*.pdf` and `pdftotext` content extracts (verified: Fig 1 title now reads "rejects H₀ (P = 0.023)" with "n=49 / n=25 / n=24 / n=17 / n=7" row labels, Fig 2 title now reads "49-node guideline tree", Fig 3 v3 point now at "0.39" / "P = 0.0231"); `data/results/v3_pooled_efdpr.json`; `data/processed/v3_nsclc_full.json` (178 trials, PAPILLON `provenance = "nsclc_v3_round1_added"` present); `data/processed/v3_combined_dag_edges.json` (212 edges; 147 NSCLC + 65 mBC verified); `analysis/v3_05_merge_and_kappa_nsclc.py` (PAPILLON still absent from `HAND_CURATED_SUPPLEMENTARIES`); `release/RELEASE_NOTES_v3.0.0.md`; `release/submission_plan_paperA_jcopo/AUDIT_REPORT.md`; `release/submission_kit_paperB_medrxiv/cover_letter_medrxiv.md`; `git log dadf797..HEAD` → one commit (`123a461`); `git tag -n5 v3.0.0` (still annotated with "P=0.0004 / 50-node / EFDPR 0.48").

The R3-integration commit `123a461` did genuine work that closes the highest-priority R3 ask: (i) **the figure-script de-hardcoding is real**: `v3_09_figures.py:44-51` now reads `nn(key) = EFDPR[key]["strict"]["total_nodes"]` and parameterises every row label; `:86-89` derives the Figure 1 title from `EFDPR["primary"]["strict"]["exact_p_one_sided_vs_p25"]`; `:126-127` derives "49-node" from `EFDPR["primary"]["strict"]["total_nodes"]`; `:144-154` reads the v3 EFDPR/P/n from the JSON; rendering produces Figure 1 with "P = 0.023", "n=49", and Figure 3 v3 point at "0.39 / P = 0.0231" (verified via `pdftotext`); (ii) **`release/canonical_numbers.md` is a real artefact** that nails down the single source of truth at 4-decimal precision; (iii) **`release/audit_numbers.sh` runs and reports PASS** across 11 release files including `paper_A_clinical_v3.tex` and `paper_B_methods_v3.tex`; (iv) `release/RELEASE_NOTES_v3.0.0.md` has been swept to the new numbers ($P = 0.023$, EFDPR 0.39, $n = 49$, 178 trials, 259 pooled) — the headline finding paragraph is now internally consistent with the released JSON. R3 asks 1 (figures), 6 (release-notes headline), and the canonical-numbers infrastructure (a new R3 contribution) are demonstrably fixed.

**However**, the R3 commit message itself catalogues seven items as deferred ("N20/N22 brain-mets evidence-free, TROPION-Lung01 N9 evidence-free, ALTA-1L temporal expulsion, SAVANNAH `investigational (other)`, NSCLC adjudication formal pass deferred, v3 supplement still missing, PAPILLON injection still post-hoc"). I find that the audit-PASS verdict is **partially load-bearing on a too-narrow forbidden-literal set**: re-grepping the manuscripts for literals the audit list does not cover surfaces five residual Paper A contradictions that R3 missed and one Paper B internal contradiction (Paper B `:130`'s "17/49 → 17/50" still echoes the dead "50" within a sentence whose first half says "49"). The release tag annotation is also still wrong (`git tag -n5 v3.0.0` continues to read "P=0.0004 ... 50-node ... EFDPR 0.48"); R3 did not cut `v3.0.1`. The PAPILLON-reproducibility breach (R1/R2/R3 ask) and the missing `supplement_v3.tex` (R1/R2/R3 ask) are also still open.

Below are 5 concrete asks ordered by severity. The R3 commit is the first R-pass that does substantive defect-class engineering (canonical_numbers + audit script + figure de-hardcoding) rather than ad-hoc literal sweeps — that infrastructure is good and should be preserved. The remaining asks are confined to (a) extending the audit's forbidden-literal set, (b) the PAPILLON reproducibility fix that has been deferred since R1, and (c) the release-tag re-cut + supplement creation.

---

## 1. Five residual Paper A stale claims that `audit_numbers.sh` does not catch (preprint OK, journal needs polish before submission)

1. **ACTION: Extend `release/audit_numbers.sh` FORBIDDEN list with five additional patterns and rewrite the five contradictions they would flag.** The five contradictions, each missed by the current audit's literal set:

   - **Paper A `:81` (Methods §2.2):** "176 systematic candidates ... brought the final NSCLC corpus to 178 trials, of which 145 entered the in-scope trial-DAG." → 176 + 5 − 1 = 180, not 178 (arithmetic error); the merge code only hand-curates 3 supplementaries (not 5) and drops 4 misidentified NCTs (not 1) per `v3_05_merge_and_kappa_nsclc.py:18,121`; and "145 entered the in-scope trial-DAG" is **stale** — the released `data/processed/v3_combined_dag_edges.json` has **147 NSCLC in-scope edges** (verified by counting `tumor == "NSCLC"`), and Paper A's own `:90` correctly says "147 NSCLC in-scope". The Paper A body therefore contradicts itself by 2 trials within the same Methods section. Rewrite to: "176 systematic candidates plus 3 hand-curated round-1 corrections (AURA3 NCT02151981, HERTHENA-Lung01 NCT04619004, TROPION-Lung01 NCT04656652) plus 1 manually-injected supplementary (PAPILLON NCT04538664) minus 4 misidentified NCTs dropped at merge yielded the final NSCLC corpus of 178 trials, 147 of which entered the in-scope trial-DAG after the drug-class filter." Add `"145 entered the in-scope"` and `"145 NSCLC"` to the audit FORBIDDEN list.

   - **Paper A `:84` (Methods §2.3):** "Annotator B (Codex / GPT-5) on a **15-trial validation subset**" → actual analyzable overlap is **13** (`v3_nsclc_kappa.json:n_validation = 13`; merge code `v3_05:138`). Also "kappa gate failed pre-adjudication on `post_alk_tki` ... and `egfr_t790m` ...; other fields passed" — Paper B `:127` discloses **three** failures including `drug_class` PABAK = 0.54; Paper A discloses only two. The asymmetry is a fact-check failure for a JCO PO reviewer reading Paper A in isolation. Either match Paper B's disclosure verbatim ("kappa gate failed pre-adjudication on `post_alk_tki` ($\kappa = 0.29$), `egfr_t790m` ($\kappa = 0.61$), and `drug_class` (PABAK = 0.54); a formal NSCLC adjudication pass is deferred to v3.0.1") or refine the gate definition. R1 ask 6, R2 asks 6+7, R3 ask 2 all flagged this; still unfixed. Add `"15-trial validation"` to the FORBIDDEN list.

   - **Paper A `:133` (Discussion Headline):** "At adequately-powered multi-tumor scale (**50 pooled guideline nodes** across mBC HR+/HER2- and NSCLC EGFR + ALK), the pre-registered exact-binomial test rejected $H_0: \mathrm{EFDPR} \le 0.25$ at $P = 0.023$." → **49** pooled guideline nodes (the abstract `:50`, Key Objective `:61`, Methods `:90`, Results `:95`, Figure 1 caption `:100`, Figure 2 caption `:123`, Figure 3 caption `:110` all correctly say 49; only the Discussion headline still says 50). The R3 audit's `"50-node"` literal does not match "50 pooled guideline nodes" because of the space and prose between "50" and "node". Add `"50 pooled"` and `"50 pooled guideline"` to the FORBIDDEN list.

   - **Paper A `:136` (Discussion EGFR cluster):** "Of 17 EGFR-mutant decision nodes, **12 are evidence-free at strict tolerance**." → **7** evidence-free of 17 per `data/results/v3_pooled_efdpr.json:nsclc_egfr_only.strict.evidence_free_count = 7` (verified — nodes N9, N10, N12, N21, N22, N23, N24 are empty; N1–N8, N11, N25 have ≥ 1 support). The 12 figure is the pre-R1 stale literal; the released JSON has been 7 since R1-integration. The audit's `"12 of 17 EGFR"` and `"12 EGFR-mutant"` literals do not match "12 are evidence-free at strict tolerance" because Paper A reverses the noun order. R2 ask 5, R3 ask 2 specifically flagged this 29-point error; still unfixed. Add `"12 are evidence-free"`, `"Of 17 EGFR-mutant decision nodes, 12"`, `"Of 17 EGFR.*12"` to the FORBIDDEN list.

   - **Paper A `:142` (Discussion §"What this means"):** "The **19** evidence-free decision nodes are not all equally problematic. Some (e.g., NCCN's recently-added TROP2-ADC nodes) ..." — this is internally consistent with the abstract (19/49), but the immediately-preceding §"Three caveats" headline at `:133` says "50 pooled guideline nodes" and the immediately-following §"NSCLC EGFR post-osimertinib" at `:136` says "12 are evidence-free". A reader who lands on the Discussion section sees 50 → 12 EGFR-evidence-free → 19 total within three paragraphs, which is incoherent. This is the highest-visibility Discussion-section defect because a JCO PO reviewer skim-reads Discussion first.

   The minimum fix is six text edits (Paper A lines 81, 84, 133, 136, plus restoring N9/N10/N12/N21/N22/N23/N24 as the seven EGFR-evidence-free node list) and four new FORBIDDEN literals in `audit_numbers.sh`. The audit script's structural soundness is good; its forbidden-literal set is too narrow. Rerun the audit after the additions and confirm it catches these five contradictions.

## 2. Paper B `:130` is internally contradictory within one sentence (`audit_numbers.sh` does not catch it; preprint blocker for Paper B)

2. **ACTION: Rewrite Paper B `:130` (Tolerance-sensitivity grid §6.3) because the sentence "ESCAT-aligned and liberal tolerance gave 0.347 (17/49 $\to$ **17/50**; $P = 0.084$)" contradicts its own first half within the same sentence.** The correct k/n is 17/49 (canonical_numbers.md row 38: "ESCAT-aligned 17/49"; verified against `v3_pooled_efdpr.json:primary.escat.evidence_free_count = 17, total_nodes = 49`); the "17/50" is the dead pre-R1 figure. Rewrite to "ESCAT-aligned and liberal tolerance gave 0.347 (17/49; $P = 0.084$), failing to reject." Add `"17/50"` and `"24/50"` to the FORBIDDEN list. Also re-verify Paper B `:124` "After adjudication, mean PABAK was 0.99 (mBC) and 0.73 (NSCLC)" — the NSCLC 0.73 is the **pre**-adjudication PABAK per `v3_nsclc_kappa.json:mean_pabak = 0.7308` (Paper B `:127` confirms there is no NSCLC adjudication pass), so the "After adjudication" framing conflates mBC's adjudicated 0.99 with NSCLC's pre-adjudication 0.73. R1 ask 14, R2 ask 5B flagged this PABAK-conflation; the R3 commit did not address Paper B `:124`. Rewrite to: "Mean pre-adjudication Cohen's $\kappa$ across key fields was 0.67 (mBC) and 0.78 (NSCLC); after the mBC adjudication pass, mean PABAK rose to 0.99 (mBC); the NSCLC corpus has no formal adjudication pass and its pre-adjudication mean PABAK was 0.73 (deferred to v3.0.1)."

## 3. PAPILLON reproducibility breach still unresolved (R1, R2, R3 unaddressed; preprint blocker only if the reproducibility commitment in Paper B `:155-156` is taken at face value; otherwise journal-only blocker)

3. **ACTION: Add PAPILLON (NCT04538664) as a fourth entry in `HAND_CURATED_SUPPLEMENTARIES` in `analysis/v3_05_merge_and_kappa_nsclc.py:18-97`.** Verified: `grep "PAPILLON\|NCT04538664"` in `v3_05_merge_and_kappa_nsclc.py` returns zero matches; `python3 -c "import json; d=json.load(open('data/processed/v3_nsclc_full.json')); print(len(d), 'NCT04538664' in [t['nct_id'] for t in d])"` returns `178 True` with `provenance = "nsclc_v3_round1_added"`. Re-running the merge script from scratch therefore produces 177 NSCLC trials (no PAPILLON), and the resulting pooled EFDPR is numerically different from what the paper reports — a hard breach of Paper B `:155-156` "The tagged commit reproduces every figure and table". R1 ask 11, R2 ask 4, R3 ask 4 all asked for this. The fix is ~10 lines of Python: a dict in the `HAND_CURATED_SUPPLEMENTARIES` list with PAPILLON's NCT, name, prior_state, biomarker tokens (EGFR-ex20ins), drug_class ("Amivantamab + chemotherapy"), and year_pc. The R3 commit's effort budget (35 lines of figure-script changes + 142 lines of audit infrastructure + 25 lines of release-notes sweep) demonstrates capacity for this fix; it was simply not prioritised. Either land the fix or — second-best — add to Paper B §3 (Dual-LLM-annotator extraction pipeline) one sentence: "PAPILLON (NCT04538664) was injected post-merge with `provenance = nsclc_v3_round1_added`; the released `v3_nsclc_full.json` therefore contains one record not regenerable from `v3_05_merge_and_kappa_nsclc.py` alone." For preprint deposit on medRxiv this disclosure is acceptable; for journal submission (especially RSM, whose reproducibility standards are explicit) the in-script fix is required.

## 4. `git tag v3.0.0` annotation, `supplement_v3.tex`, and four other long-standing deferred items (R1+R2+R3 unaddressed; preprint deposit OK with caveat; journal submission needs all of these)

4. **ACTION: Address the four R1/R2/R3 long-standing deferred items as a single batch before journal submission. None individually blocks medRxiv deposit; all four together will block desk-screen at RSM and JCO PO.**

   - **(4a) Re-cut `v3.0.1` with a new annotated tag.** Current `git tag -n5 v3.0.0` annotation still reads "v3.0.0 — multi-tumor pooled REJECTS H0 at P=0.0004 (citation-polished) 50-node pooled mBC + NSCLC EGFR/ALK; strict-tolerance EFDPR 0.48, exact P = 0.0004 rejects H0:EFDPR<=0.25." A reviewer who opens the GitHub release page sees three forbidden literals in the tag annotation within 3 seconds. Recommend `v3.0.1` annotation: "v3.0.1 — multi-tumor pooled REJECTS H0 at P = 0.023 (strict tolerance); ESCAT/liberal P = 0.084 (marginal non-rejection); 178 NSCLC + 81 mBC trials (259 pooled); 49-node pooled tree after R1 N13/N14 dedup; bootstrap CI 0.25–0.53." R1 ask 1 (release tag arithmetic), R2 ask 8, R3 ask 6 all asked for this.

   - **(4b) Create `manuscript/supplement_v3.tex`.** Paper A `:90` cites "Supplementary Table S1" but only `supplement.tex` (v1) and `supplement_v2.tex` (v2) exist on disk; the cross-reference is a broken pointer that a desk-editor catches immediately. Minimum content (R1 ask 16, R2 ask 9, R3 ask 5b): S1 PRISMA-2020 with graph-encoding addendum; S2 per-NCT NSCLC corpus with extraction flags; S3 NSCLC 24-node decision-tree encoding with sources; S4 NSCLC κ + PABAK by field (3 of 8 below 0.70); S5 per-node `Supp(g)` set from `v3_pooled_efdpr.json:per_node_support`. The pre-existing v2 supplement is a usable template.

   - **(4c) Update `release/RELEASE_NOTES_v3.0.0.md:9` from "25 NSCLC EGFR/ALK" to "24 NSCLC EGFR/ALK after N13/N14 dedup".** Verified: line 9 currently reads "25 mBC HR+/HER2- + 25 NSCLC EGFR/ALK" but the dedup at `v3_06_encode_nsclc_guideline.py:151-162` merged N14 into N13, leaving 24 NSCLC guideline nodes (canonical_numbers.md row 19). Also fix `:9` "210-edge trial-DAG" → "212-edge trial-DAG" (`canonical_numbers.md` row 16; verified by counting `data/processed/v3_combined_dag_edges.json`). Also fix `:65` "failed gate on `akt_path` and `post_alk_tki`" — `akt_path` is the **mBC** field that failed in v2; **NSCLC** failed on `post_alk_tki`, `egfr_t790m`, and `drug_class` per Paper B `:127`. The current release notes silently inherit v2's failure list. Add `"25 NSCLC"`, `"210-edge"`, and `"akt_path"` (when paired with NSCLC) to `audit_numbers.sh` FORBIDDEN.

   - **(4d) Document the NSCLC adjudication-formal-pass deferral, the single-annotator NSCLC tree, and the SAVANNAH/N20/N22/N9/N21 `investigational (other)` collapse in a unified "Methodological caveats" paragraph in Paper A.** The R3 commit message lists these as deferred; Paper A currently has §"Three caveats" at `:144` that does not mention any of them. Add a fourth caveat: "Fourth, the v3 NSCLC arm did not undergo a formal adjudication-rule pass parallel to v2 mBC; the pre-adjudication kappa gate failed on `post_alk_tki`, `egfr_t790m`, and `drug_class` (PABAK = 0.54); the 24-node NSCLC decision tree was hand-encoded by a single annotator without independent re-encoding; and seven NSCLC pivotal trials (SAVANNAH, ORCHARD, WU-KONG1, REZILIENT1, EXCLAIM-2, AZD3759, PAPILLON) are labelled `drug_class = investigational (other)` in the corpus, with the matcher's equivalence-table augmentation as the agreed-upon compromise. These are deferred to v3.0.1." This single paragraph closes four R1/R2/R3 deferred disclosure asks in one stroke.

## 5. Outstanding R3-deferred items: blocking-for-preprint vs blocking-for-journal triage

5. **ACTION: Read this triage list before deciding whether to deposit at medRxiv now or defer.** The R3 commit message catalogues seven deferred items. Per-item triage:

   | R3-deferred item | medRxiv preprint deposit | JCO PO + RSM journal submission | Honest disclosure status |
   |---|---|---|---|
   | N20/N22 brain-mets evidence-free | not blocking | not blocking (these are real evidence gaps; the framework correctly identifies them) | honest — Paper A `:118` lists N22, and the JSON confirms |
   | TROPION-Lung01 N9 evidence-free (post_osimertinib null) | not blocking | not blocking (TROPION-Lung01 is `tumor_type = NSCLC-other` per `v3_05:88`, so strict matcher does not route it to N9; this is a real downstream consequence of the corpus filter) | honest — Paper A `:136` lists N9 as evidence-free; reviewer can verify |
   | ALTA-1L temporal expulsion | not blocking | not blocking (ALTA-1L year_pc=2020 satisfies temporal precedence vs guideline year; not actually expelled — confirmed by per_node_support N15) | not actually an issue (R3 commit-message item appears to be over-cautious) |
   | SAVANNAH `investigational (other)` | not blocking | needs disclosure for RSM (R3 ask 7) | partial — Paper A `:136` cites SAVANNAH as a "post-osimertinib decision space" example without acknowledging that the corpus labels it `investigational (other)`. Should be folded into ask 4d above. |
   | NSCLC adjudication formal pass deferred | not blocking | needs disclosure (R1 ask 4) | partial — Paper B `:127` discloses; Paper A does not. Ask 1 above. |
   | v3 supplement still missing | not blocking (medRxiv permits supplement at submission) | **blocking** for JCO PO submission (Paper A cites Supplementary Table S1) | ask 4b above |
   | PAPILLON injection still post-hoc | not blocking for medRxiv (PAPILLON is in the released artefact and the result is honest); reproducibility commitment Paper B `:155-156` is contingent on whether reviewer re-runs the script | **blocking** for RSM (whose reproducibility standards are explicit) | dishonest as currently presented (Paper B claims script reproduces all numbers; it does not) — ask 3 above |

   **Net assessment**: the manuscript is honest enough for medRxiv preprint deposit *if* asks 1 (Paper A residual stale literals) and 2 (Paper B `:130` self-contradiction + `:124` PABAK-conflation) are fixed first. Ask 3 (PAPILLON), 4 (release tag + supplement + release-notes + caveats consolidation) should be completed before journal submission to either RSM or JCO PO.

---

## Verdict

**MINOR**

The R3-integration commit `123a461` is the first R-pass that does **defect-class engineering** rather than ad-hoc literal sweeps: it introduces `release/canonical_numbers.md` (a single source of truth at 4-decimal precision), `release/audit_numbers.sh` (a programmatic CI-style stale-literal grep that currently reports PASS across 11 release files), and a genuine de-hardcoding of `analysis/v3_09_figures.py` such that all three v3 figure PDFs now render the correct $P = 0.023$, $n = 49$, EFDPR 0.39 (verified via `pdftotext`). The headline finding ($P = 0.023$ strict rejection; $P = 0.084$ ESCAT/liberal marginal non-rejection), the prereg trail (`085ae54` → `079b540` → `4b5bf1a`, all before outcome data), the bootstrap CI [0.245, 0.531], the dual-annotator pipeline with documented κ/PABAK failures, and the released JSONs (`v3_pooled_efdpr.json`, `v3_nsclc_kappa.json`, `v3_combined_dag_edges.json`) are internally consistent and honest. Paper A's abstract `:50`, Key Objective box `:61`, Methods §2.5 `:90`, Results `:95`, all three figure captions `:100,:110,:123` and Paper B's abstract `:48` and Demonstration §6.2 `:127` are now consistent with `canonical_numbers.md` and the released JSON to 4 decimal places. **The R3 commit demonstrably moved the manuscript from MAJOR-blocking to a state where medRxiv preprint deposit is defensible once the five residual Paper A stale literals (ask 1), the Paper B `:130` self-contradiction (ask 2), and the Paper B `:124` PABAK-conflation are fixed.** These three asks together are < 30 lines of edits and one re-run of `audit_numbers.sh` with an extended FORBIDDEN set.

The PAPILLON reproducibility breach (ask 3), the release-tag re-cut (ask 4a), the missing `supplement_v3.tex` (ask 4b), the release-notes residual stale numbers (ask 4c), and the consolidated methodological-caveats paragraph (ask 4d) can be addressed in the 14-day polish window pre-submission to JCO PO + RSM. None of these is a preprint blocker; all five are journal-submission blockers (especially for RSM, whose AI-transparency + reproducibility standards are the highest in the methods-journal landscape).

**Preprint disclosure honesty**: with ask 1 + ask 2 fixed, the manuscript is honest enough for medRxiv deposit; the PAPILLON-reproducibility issue (ask 3) is a known limitation that can be flagged in the deposit cover letter and resolved before journal submission. **Without ask 1 + ask 2 fixed**, the Discussion-section "50 pooled / 12 EGFR-evidence-free / 19 evidence-free" contradiction within three paragraphs is a desk-editor catch in 60 seconds and should be fixed before deposit.

The R3 commit is the right kind of work (programmatic, auditable, source-of-truth-driven); the remaining gap is that the FORBIDDEN literal set in `audit_numbers.sh` is too narrow and missed five of the highest-visibility Paper A Discussion-section contradictions and one Paper B intra-sentence contradiction. Extending the FORBIDDEN list per ask 1 + ask 2 will catch them in CI and prevent a v4 reviewer-cycle on the same defect class.

**VERDICT: MINOR**
